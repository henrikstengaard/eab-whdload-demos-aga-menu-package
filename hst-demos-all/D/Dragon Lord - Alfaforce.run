; Hst Launcher run script
; -----------------------
;RunFile=A-Demos:D/DragonLord/DragonLord.Slave

; Run WHDLoad
cd "A-Demos:D/DragonLord/"
IF EXISTS ENV:whdloadargs
  whdload "DragonLord.Slave" $whdloadargs
ELSE
  whdload "DragonLord.Slave"
ENDIF
