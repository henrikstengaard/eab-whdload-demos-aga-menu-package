; Hst Launcher run script
; -----------------------
;RunFile=A-Demos:W/WildAGA/WildAGA.Slave

; Run WHDLoad
cd "A-Demos:W/WildAGA/"
IF EXISTS ENV:whdloadargs
  whdload "WildAGA.Slave" $whdloadargs
ELSE
  whdload "WildAGA.Slave"
ENDIF
