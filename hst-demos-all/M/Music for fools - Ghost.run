; Hst Launcher run script
; -----------------------
;RunFile=A-Demos:M/MusicForFools/MusicForFools.Slave

; Run WHDLoad
cd "A-Demos:M/MusicForFools/"
IF EXISTS ENV:whdloadargs
  whdload "MusicForFools.Slave" $whdloadargs
ELSE
  whdload "MusicForFools.Slave"
ENDIF
