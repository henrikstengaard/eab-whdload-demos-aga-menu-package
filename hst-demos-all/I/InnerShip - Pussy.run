; Hst Launcher run script
; -----------------------
;RunFile=A-Demos:I/InnershipPussy/InnershipPussy.slave

; Run WHDLoad
cd "A-Demos:I/InnershipPussy/"
IF EXISTS ENV:whdloadargs
  whdload "InnershipPussy.slave" $whdloadargs
ELSE
  whdload "InnershipPussy.slave"
ENDIF
